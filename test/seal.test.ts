import { describe, it } from 'vitest';

describe('seal / verifySeal', () => {
  it.todo('sealed file decrypts correctly');
  it.todo('sealed file detects rollback');
  it.todo('sealed file detects truncation');
  it.todo('unsealed files still work');
});
